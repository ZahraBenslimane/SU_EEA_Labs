#include <iostream>

#include "CBasePile.h"
#include "CPileFifo.h"
#include "CPileLifo.h"



int main() {


//	CBasePile* ptPile;
//	CPileFifo fifo; // pile First In First Out
//	CPileLifo lifo; // pile Last In First Out
//	ptPile = &fifo; // ptPile pointe vers la pile FIFO
//	*ptPile < 0 < 1 < 8 < 9 < 0; // on empile 0 puis 1
//	cout << "pile=" << *ptPile; // à l’écran : pile=0 1
//	int i, j;
//	*ptPile > i> j; // dépile dans i
//	cout << "pile=" << *ptPile << "i=" << i; // à l’écran : pile=1 i=0
//	ptPile = &lifo; // ptPile pointe vers la pile LIFO
//	*ptPile < 0 < 1 < 0< 2; // on empile 0 puis 1
//	cout << "pile=" << *ptPile; // à l’écran : pile=0 1
//	*ptPile > i> j; // dépile dans i
//	cout << "pile=" << *ptPile << " i=" << i << endl; // à l’écran : pile=0 i=1


	return 0;
}
